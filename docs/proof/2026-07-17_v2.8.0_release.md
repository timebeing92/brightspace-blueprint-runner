# Blueprint Wizard v2.8.0 Release Receipt

Date: 2026-07-17 (America/New_York)

Status: **published and independently re-downloaded successfully**.

## Immutable source pair

- Blueprint Wizard tag `v2.8.0`:
  `4dfa5fd349c7e58b2bf9deb31fc5a78439134d60`
- Blueprint Bundle tag `v1.3.0`:
  `0b197d95b8b1a5593f92772e664b1d04c5441677`
- Stable launcher version: `1.0.0`
- Release-manifest protocol: `coursecraft.runner_release/1`
- Managed-launcher protocol: `1`

The Runner and Bundle default branches were fast-forwarded to these source
commits before the annotated tags were pushed. Release construction used the
full commits as refs, matching the established provenance convention.

## Public artifacts

Bundle release:
https://github.com/timebeing92/brightspace-blueprint-bundle/releases/tag/v1.3.0

- `brightspace-blueprint-bundle-v1.3.0.tar.gz`
  - SHA-256:
    `342a64a72862db541dde3dc7c168b1c3a1566dc4c1efc2d1e0321e8a3b4cd490`

Wizard release:
https://github.com/timebeing92/brightspace-blueprint-runner/releases/tag/v2.8.0

- `blueprint-wizard-managed-v2.8.0.zip`
  - SHA-256:
    `92056a7ec1cf174788883c0813f2d6abac223e4d4a7cd47d7522ef1017827541`
- `blueprint-wizard-v2.8.0.zip`
  - SHA-256:
    `8f7998a153103d059cfeb0d9cd1c36dc6d885e85596a3e95d093d7795de94669`

Each artifact was built twice from the clean source pair. The duplicate builds
were byte-identical. After publication, all three assets and their `.sha256`
sidecars were downloaded from GitHub. Recalculated hashes, sidecar checks, and
GitHub API `digest` values all agreed with the local release builds.

## Automated gates

- Bundle: 79 tests passed locally; tag CI run `29627091966` passed tests,
  explicit-ref packaging, and sidecar verification.
- Runner: 74 tests passed locally; tag CI run `29627091927` passed the full
  test/package job and managed-package launch matrix on macOS, Ubuntu, and
  Windows. The Windows job also parsed the PowerShell launcher successfully.
- CI uses `actions/checkout@v6` and `actions/setup-python@v6`; both Runner
  checkout sites pin the exact Bundle v1.3.0 commit rather than a mutable ref.

## Clean-install and end-to-end proof

The final managed ZIP was extracted into an empty temporary directory with the
system `unzip`, which restored executable mode on `Blueprint Wizard.command`
and `blueprint_wizard_launcher.sh`. The stable launcher reported the exact
source pair and `blueprint-wizard-stable-launcher 1.0.0`.

`--doctor --fix --yes --plain --no-update-check` then:

- selected non-Anaconda `/usr/local/bin/python3.13`;
- created the missing private Bundle `.venv` inside `versions/2.8.0/`;
- installed only `requirements.txt` and its dependencies;
- passed a second Doctor check; and
- passed `python -m pip check` with no broken requirements.

The managed launcher then processed the committed sample export. All 10 steps
completed, the run status was `ok`, all primary and rubric artifacts were
emitted, and structural DOCX QA reported 0 breaks and 0 warnings. User data,
logs, and output remained under the managed `user-data/` tree.

The emitted `coursecraft.run/1` receipt validated against its schema and all 23
artifact checksums. Its producer record contained Bundle v1.3.0, exact commit
`0b197d95b8b1a5593f92772e664b1d04c5441677`, identity state `release`, and
release-manifest scope `enclosing_runner_release`. A separate portable-layout
sample run produced the same exact Bundle identity.

## Live managed-update proof

An empty temporary install used the public latest-release API after v2.8.0 was
published. The updater selected the portable v2.8.0 payload, compared the API
digest with the downloaded archive, verified the independent sidecar, validated
the archive and embedded manifest, installed the complete pair, and activated
v2.8.0. Its receipt reported identical API and archive SHA-256 values:

`8f7998a153103d059cfeb0d9cd1c36dc6d885e85596a3e95d093d7795de94669`

Health, version listing, and a launched `--version` command all passed against
the downloaded installation.

## Distribution boundary

The managed and portable ZIPs are unsigned and are not notarized. The release
page, package `START_HERE.txt`, README, and signing decision note disclose the
expected Gatekeeper, SmartScreen, or institutional trust prompt. This is
accepted for the current small, named internal user group; broader distribution
remains gated on a future signing/notarization decision.
